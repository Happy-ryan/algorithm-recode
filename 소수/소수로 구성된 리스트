N = int(input())
arr = []
for n in range(2, N + 1):
	is_prime = True
	for i in range(2, n):
		if n % i == 0:
			is_prime = False
			break
	if is_prime:
		arr.append(n)
sum = 0
for j in range(len(arr)) :
    sum += arr[j]
print(sum)
print(arr[j])
